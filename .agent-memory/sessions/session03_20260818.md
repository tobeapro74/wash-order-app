# Session 03 — 2026-08-18

## 세션 목표
- 룰렛 SVG 컴포넌트 → 로또볼 머신 영상 컴포넌트로 교체
- 4번 순차 뽑기 (중복 없음) 구현
- GO 버튼 → 영상 재생 연동

## 완료된 작업

### 1. 로또볼 머신 영상 추가
- 영상: `/Users/byungchulpark/Downloads/앱아이콘/로또_영상.mp4` → `public/lottery-machine.mp4` (1.8MB)
- poster: `ffmpeg`로 첫 프레임 추출 → `public/lottery-machine-poster.jpg`

### 2. LotteryMachine.tsx 신규 생성 (`src/app/today/LotteryMachine.tsx`)
- **1차**: 단순 1회 재생 → 결과 구조 (GO 재생 안 되는 문제 있었음)
- **2차 (최종)**: 4번 순차 뽑기 구조로 전면 재작성
  - 시작 시 `makeShuffledOrder()`로 4개 순서 미리 결정 (Fisher-Yates, 중복 없음)
  - GO 버튼 → `video.currentTime = 0` + `video.play()` → `onEnded` → 1개 공개
  - 4번 반복 후 최종 결과 카드
  - 진행 바 (4칸) + 지금까지 뽑힌 결과 + 남은 슬롯 점선 표시
  - `preload="auto"` 로 재생 즉시성 확보

### 3. today/page.tsx 교체
- `RouletteWheel` → `LotteryMachine` import 변경
- `<RouletteWheel myOrder={myOrder} onResult={handleResult} />` → `<LotteryMachine onResult={handleResult} />`
- 기존 투표/결과(result/voted) 플로우는 그대로 유지

## 핵심 구조
```
idle → GO버튼 클릭 → playing(영상재생) → onEnded → 결과 1개 공개
→ step < 4: idle 복귀 (다음 GO 버튼)
→ step === 4: result (최종 순서 표시)
```

## 핵심 결정사항
- 영상은 순수 연출, 결과는 코드에서 직접 랜덤 계산
- 4번 모두 다른 항목 (양치/얼굴/머리/몸 각 1회씩 = 순열)
- `fullOrderRef`에 미리 계산된 순서 저장, stepRef로 단계 추적

## 남은 작업
- [ ] 온보딩 점 인디케이터 스크롤 연동
- [ ] 모바일 실기기 로또볼 영상 재생 최종 검증
